// Slots labeling training. P_SIZE ->t_size, P_KIND -> t_kind, P_TOPPINGS -> t_toppings, ...
.train
    ORDER_PIZZA: @i @want some @pizza @please

    ORDER_PIZZA: @pizza (@please|)

    ORDER_PIZZA: @want P_SIZE{t_size} P_KIND{t_kind} @pizza with P_TOPPINGS{t_toppings}

    ORDER_PIZZA: P_SIZE{t_size} (please|)

    ORDER_PIZZA: P_KIND{t_kind}

    ORDER_PIZZA: with P_TOPPINGS{t_toppings} (on top|)

    ORDER_PIZZA: (live in|) P_ADDRESS{t_address}

    ORDER_PIZZA_YES: ASK_TO_CONFIRM @yes

    ORDER_PIZZA_NO: ASK_TO_CONFIRM @no

    CANCEL: @cancel

.prompt

    // NOTE! Prompt here is the utterance for next layer(!!!)

    // Generate Prompt that contains only one missing slot. This is to train next dialog layer
    // See bot.txt file.
    ORDER_PIZZA: {$!t_kind|t_size|t_toppings|t_address|none}

    ORDER_PIZZA_YES: cmd_agreed
    ORDER_PIZZA_NO: cmd_disagreed
    CANCEL:cmd_cancel
